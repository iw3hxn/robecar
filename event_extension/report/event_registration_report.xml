<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="event.attendee_list" model="ir.ui.view">
        <field name="name">attendee_list</field>
        <field name="type">qweb</field>
        <field name="arch" type="xml">
            <t t-name="event.attendee_list">
                <h1>Attendee list</h1>
                <div class="row">
                    <div class="col-7">
                        <div t-out="event.name"/>
                    </div>
                    <div class="col-5">
                        <span t-out="event.date_begin_located"/>
                        <i class="fa fa-arrow-right"/>
                        <span t-out="event.date_end_located"/>
                    </div>
                </div>
                <tr t-foreach="attendees.grouped('position_id').items()" t-as="position">
                    <t t-set="table_name" t-value="position[0]"/>
                    <t t-set="registrations" t-value="position[1]"/>
                    <div class="col-7">
                        <div t-out="table_name.display_name or ''"/>
                    </div>
                    <table class="table mt-3" style="page-break-after:always;">
                        <thead>
                            <tr class="text-start">
                                <th>Name</th>
                                <!-- Add dynamic question columns -->
                                <t t-foreach="event.question_ids.filtered(lambda q: q.question_type in ['simple_choice', 'text_box'])" t-as="question">
                                    <th t-out="question.title"/>
                                </t>
                            </tr>
                        </thead>
                        <tbody>
                            <tr t-foreach="registrations" t-as="registration">
                                <td><t t-out="registration.name"/></td>
                                <!-- Add dynamic answer columns -->
                                <t t-foreach="event.question_ids.filtered(lambda q: q.question_type in ['simple_choice', 'text_box'])" t-as="question">
                                    <td>
                                        <t t-set="answer" t-value="registration.registration_answer_ids.filtered(lambda a: a.question_id == question)"/>
                                        <t t-if="answer">
                                            <t t-out="answer.display_name"/>
                                        </t>
                                    </td>
                                </t>

                            </tr>
                        </tbody>
                    </table>
                </tr>
            </t>
        </field>
    </record>
</odoo>